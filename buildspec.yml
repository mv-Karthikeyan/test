version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18.20.3
    commands:
      - echo "Installing dependencies..."
      - npm install

  build:
    commands:
      - npm run build

artifacts:
  files:
    - '.next/**/*'

cache:
  paths:
    - node_modules/**/*
